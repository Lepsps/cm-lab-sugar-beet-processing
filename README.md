# СППР "Переработка Сахарной Свеклы"

<p align="center">
  <img src="assets/Снимок экрана 2025-12-11 031340.png" alt="Project Banner" width="600">
</p>

Это десктопное приложение — система поддержки принятия решений (СППР), разработанная для моделирования и анализа стратегий переработки сахарной свеклы. Программа позволяет проводить виртуальные эксперименты, сравнивать эффективность различных эвристических подходов с математически оптимальным решением и рекомендовать лучшую стратегию на основе заданных параметров.

Приложение имеет высокопроизводительное ядро, написанное на **C++** для ускорения вычислений, и графический интерфейс на **Python (PyQt5)**.



## 🚀 Основные возможности

*   **Графический интерфейс:** Удобный интерфейс для ввода параметров и визуализации результатов.
*   **Гибридный подход:** Логика интерфейса на Python, все ресурсоемкие вычисления вынесены в скомпилированный C++ модуль.
*   **Детальное моделирование:** Учитываются такие факторы, как:
    *   Начальная сахаристость (`alpha`).
    *   Коэффициенты деградации (`beta`).
    *   Процесс дозаривания (рост сахара).
    *   Влияние неорганических веществ (потери при переработке).
*   **Сравнение стратегий:** Реализовано 5 эвристических стратегий и 1 точный (оптимальный) алгоритм:
    1.  **Жадная** (Greedy)
    2.  **Бережливая** (Thrifty)
    3.  **Медианная** (Median)
    4.  **Жадно-бережливая** (Greedy-Thrifty)
    5.  **Бережливо-жадная** (Thrifty-Greedy)
    6.  **Точный алгоритм** (Венгерский) для нахождения эталона.
*   **Настраиваемый интерфейс:** Возможность переключения между темной/светлой темой и выбора цветовой гаммы.
*   **Асинхронность:** Вычисления не блокируют интерфейс, процесс можно отменить в любой момент.
*   **Тестирование:** Проект покрыт модульными тестами на C++ и Python.

## ⚙️ Технологический стек

*   **Бэкенд (ядро):** C++17
*   **Фронтенд (GUI):** Python 3, PyQt5
*   **Связка C++/Python:** Pybind11
*   **Графики:** Matplotlib
*   **Вычисления:** NumPy
*   **Тестирование:** Pytest (для Python), встроенный тестовый модуль (для C++)

## 🔧 Установка и запуск

### Шаг 1: Требования

Перед запуском убедитесь, что у вас установлены:
1.  **Python** (версия 3.8 или новее).
2.  **Компилятор C++:**
    *   **Windows:** [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/) (при установке выберите "Разработка классических приложений на C++").
    *   **macOS:** `xcode-select --install` в терминале.
    *   **Linux:** `sudo apt install build-essential` (для Debian/Ubuntu).

### Шаг 2: Установка зависимостей Python

Откройте терминал в корневой папке проекта и выполните команду:
```bash
pip install -r requirements.txt
```

### Шаг 3: Компиляция C++ модуля

В той же папке выполните команду для сборки C++ ядра. Это создаст файл `sugar_core.pyd` (или `.so` на Linux).

```bash
python setup.py -q build_ext --inplace
```
> **Важно!** Если во время компиляции возникают ошибки, убедитесь, что C++ компилятор установлен корректно и доступен в системных путях.

### Шаг 4: Запуск приложения

После успешной компиляции запустите главный файл:
```bash
python main.py
```

## 🧪 Запуск тестов

Для проверки корректности логики можно запустить тесты.

### C++ тесты (внутренние)
Проект также содержит внутренние тесты на C++. Их можно запустить отдельным скриптом (необходимо создать).

## 📖 Как пользоваться программой

1.  **Панель настроек (слева):**
    *   **Общие параметры:** Задайте количество экспериментов `(T)` и партий/этапов `(n)`.
    *   **Параметры сырья:** Укажите диапазоны для начальной сахаристости (`alpha`) и коэффициентов деградации (`beta`).
    *   **Дозаривание:** Включите, если хотите моделировать рост сахара в начале сезона.
    *   **Химия:** Включите, чтобы учесть потери сахара от неорганических веществ.
2.  **Запуск:** Нажмите зеленую кнопку **"ЗАПУСТИТЬ МОДЕЛИРОВАНИЕ"**.
3.  **Отмена:** Во время вычислений появится красная кнопка **"ОТМЕНИТЬ"** для прерывания процесса.
4.  **Результаты (справа):**
    *   На **графике** будет показано, на сколько процентов каждая стратегия проигрывает идеальному решению (чем ниже столбец — тем лучше).
    *   В **текстовом поле** будет выведен отчет со средними потерями и итоговая рекомендация.
5.  **Настройки темы (⚙️):** Нажмите на иконку шестеренки в левом нижнем углу, чтобы изменить цветовую гамму приложения.
